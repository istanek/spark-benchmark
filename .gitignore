# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
*.egg
.Python
build/
dist/
develop-eggs/
.eggs/
.installed.cfg
*.so
pip-log.txt
pip-delete-this-directory.txt

# Virtual environments
.venv/
venv/
env/
ENV/

# Test / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/
coverage.xml

# Type checkers
.mypy_cache/
.ruff_cache/
.pyre/
.pytype/

# Editors / OS
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Benchmark output (committing report templates is fine; raw runs aren't)
results/runs/
results/benchmarks/
results/custom/
**/results.jsonl

# Quick-run prompts persisted via `spark-bench quick --save` or the
# TUI's Quick → "Save as suite?" affordance. Kept out of the repo so
# personal one-shots don't end up under version control alongside the
# shipped templates.
examples/custom-tests/quick-saved/

# Local secrets / overrides
.env
.env.local
*.pem
*.key
